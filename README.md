# Text Share

## DB(prisma+accelerate)の起動
```bash
npx prisma-accelerate-local postgresql://user:password@postgres.database.local:5432/mydatabase?schema=public -p 8000
```


formatte
```bash
npx biome check --write ./src
```